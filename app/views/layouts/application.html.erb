<%= render 'layouts/header' %>
      <% flash.each do |key, value| %>
        <div class="flash <%= key%>"><%= value %></div>
      <% end %>
      <div class="span10">
        <h2><%= @titulo %></h2>
        <%= yield %>
      </div>
      <div class="span4">
        <%= render 'layouts/menu' %>
      </div>
      
<%= render 'layouts/footer' %>