<%= render 'layouts/header' %>
      <% flash.each do |key, value| %>
        <div class="flash <%= key%>"><%= value %></div>
      <% end %>
      <div class="span10">
        <h2><%= @titulo %></h2>
        <%= yield %>
      </div>
      
<%= render 'layouts/footer' %>